# Keys `check-i18n.mjs` cannot see, each with the reason it is invisible.
# Anything listed here that becomes referenced fails the check too, so the
# list cannot rot into a way of keeping dead sentences alive.

# Assembled from a direction at run time: `t(`rateHistory.delta.${direction}`)`
# in RateHistoryTable's `Delta`, which is `up` or `down`.
rateHistory.delta.up
rateHistory.delta.down
rateHistory.delta.upLabel
rateHistory.delta.downLabel

# Assembled from a dependency's name: `t(`ops.health.${name}`)` in the
# Operations page's `HealthCard`, for the two stores it describes.
ops.health.redis
ops.health.mongodb
